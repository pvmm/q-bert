#ifndef _MSXGL_CORE_H
#define _MSXGL_CORE_H

#include "config_option.h"      // Configuration options

typedef unsigned char c8;       // 8 bits character type

#define loop(a, b)          for(uint8_t a = 0; a < b; ++a)

#endif // _MSXGL_CORE_H
